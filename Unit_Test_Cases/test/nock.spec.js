const sinon = require('sinon');
const chai = require('chai');
const nock = require('nock');
const expect = chai.expect;

const { Marks } = require('../controller/studentCon');
const marksObj = new Marks();


describe('This is Nock Test', () => {

    // it('api test', (done) => {
    //     let obj = { }
    // });

});